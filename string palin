#include<stdio.h>
#include<string.h>
int main(){
	int len,count=0,i;
	char s1[100];
	scanf("%s",s1);
	len=strlen(s1);
	for(i=0;i<len;i++){
		if(s1[i]==s1[len-i-1]){
			count++;
		}
	}if(len==count){
		printf("palindrome");
	}else{
		printf("not");
	}
	return 0;
}
